# --------------------
# Instructions for Dockerfile to create a new image on a base image (openjdk)
# Using base image openjdk:latest
FROM openjdk:latest
# Copying myfile1.txt to containers /usr/share directory
Copy *.txt /usr/share
